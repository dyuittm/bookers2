<div class="container">
  <div class="row">
    <div class="col-lg-4">
      <%= render 'books/sideform', book: @book, user: @user %>
    </div>
    <div class="col-lg-8">
      <h4>Books</h4>
      <table>
        <thead>
          <tr>
            <th colspan=3></th>
            <th>Title</th>
            <th>Opinion</th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
            <tr>
              <td><%= image_tag book.user.get_profile_image(100,100) %></td>
              <td><%= link_to book.title, book_path(book.id) %></td>
              <td><%= book.body %></td>
            </tr>
          <% end %>
          <%= paginate @books %>
        </tbody>
      </table>
    </div>
  </div>
</div>
